import 'package:flutter/material.dart';

class ProfileLettersTab extends StatelessWidget {
  const ProfileLettersTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Profile letters'),
    );
  }
}
