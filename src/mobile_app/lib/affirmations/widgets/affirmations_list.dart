import 'package:flutter/material.dart';

class AffirmationsList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Affirmations list'),
    );
  }
}